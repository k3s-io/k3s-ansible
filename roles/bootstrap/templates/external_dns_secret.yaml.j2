{% if external_dns_secret_name and external_dns_secret_data %}
---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ external_dns_secret_name }}"
type: Opaque
stringData:
  {{ external_dns_secret_data | to_nice_yaml | trim | indent(2)}}
{% endif %}

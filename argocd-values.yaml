dex:
  enabled: false
server:
  ingress:
    enabled: true
    https: false
    hosts:
      - argocd.k8s-frambozen.net
  extraArgs:
    - --insecure
  config:
    repositories: |
      - type: helm
        name: argo-cd
        url: https://argoproj.github.io/argo-helm
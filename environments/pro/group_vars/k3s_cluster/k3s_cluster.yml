---
# Define all the specific k3s variables
k3s_cluster:
  version: v1.30.2+k3s1
  api_endpoint: "{{ hostvars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"
  token: "mijn_k3s_token"  # Use ansible vault if you want to keep it secret
  use_external_databases: true
  extra_server_args: '--datastore-endpoint "mysql://k3s-pro:Ia!UeE15))LcA1q.@tcp(192.168.1.72:3306)/k3s-pro"'
  extra_agent_args: ""
